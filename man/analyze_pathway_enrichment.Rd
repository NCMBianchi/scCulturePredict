% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathway_analysis.R
\name{analyze_pathway_enrichment}
\alias{analyze_pathway_enrichment}
\title{Analyze pathway enrichment}
\usage{
analyze_pathway_enrichment(
  seurat_obj,
  kegg_pathways,
  min_genes = 5,
  max_genes = 500,
  p_cutoff = 0.05,
  verbose = TRUE
)
}
\arguments{
\item{seurat_obj}{Seurat object. The Seurat object containing gene expression data.}

\item{kegg_pathways}{List. A list of KEGG pathways where each element is a character
vector of gene names. The names of the list should be pathway IDs.}

\item{min_genes}{Integer. Minimum number of genes required in a pathway for analysis
(default: 5).}

\item{max_genes}{Integer. Maximum number of genes allowed in a pathway for analysis
(default: 500).}

\item{p_cutoff}{Numeric. P-value cutoff for significant pathways (default: 0.05).}

\item{verbose}{Logical. Whether to print progress messages (default: TRUE).}
}
\value{
A data frame containing pathway enrichment results with columns:
\itemize{
  \item pathway: Pathway ID
  \item n_genes: Number of genes in the pathway
  \item mean_expr: Mean expression of pathway genes
  \item sd_expr: Standard deviation of pathway expression
  \item p_value: P-value from statistical test
  \item adj_p_value: Adjusted p-value (FDR)
}
}
\description{
Performs pathway enrichment analysis on a Seurat object using KEGG pathways.
This function calculates the mean expression of genes in each pathway and
performs statistical testing to identify significantly enriched pathways.
}
\details{
The function performs the following steps:
\enumerate{
  \item Validates input parameters and data
  \item Filters pathways based on gene count
  \item Calculates mean expression for each pathway
  \item Performs statistical testing
  \item Adjusts p-values for multiple testing
}
}
\examples{
# Example with mock pathway activity matrix
pathway_activities <- matrix(
  rnorm(150),
  nrow = 15,
  dimnames = list(
    paste0("Pathway", seq_len(15)),
    paste0("Sample", seq_len(10))
  )
)
plot <- create_pathway_heatmap(pathway_activities)
}
\seealso{
\code{\link{create_pathway_heatmap}} for visualizing pathway results
\code{\link{analyze_pathway_activity}} for analyzing pathway activity
}
